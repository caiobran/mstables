SELECT id, ticker
	FROM Tickers
	WHERE ticker NOT IN (
		SELECT Tickers.ticker as symbol
			FROM Fetched_urls JOIN Tickers ON Fetched_urls.ticker_id = Tickers.id
			WHERE strftime('%Y%m', Fetched_urls.fetch_date) = strftime('%Y%m', 'now') AND Fetched_urls.url_id = 1)
	ORDER BY ticker ASC
